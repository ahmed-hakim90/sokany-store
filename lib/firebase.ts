/**
 * @deprecated Prefer `@/lib/firebase-app` for app/messaging to keep the client bundle small.
 * Auth helpers live in `@/lib/firebase-client-auth` (dynamic `firebase/auth`).
 */
export { getFirebaseApp, getFirebaseMessaging } from "@/lib/firebase-app";
